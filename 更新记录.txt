2026.03.15
v1.00：
初版。
v1.11：
定时任务可采用调用会话中和独立LLM两种方式，前者更符合kriaai精神和更真人感。
2026.03.16
v1.12：
修复查看工具无法正确获取其他用户说说、回复正确tid的问题，从而修复评论他人功能、定时回复的格式。
v1.15：
-查看工具显示时间戳。
-新增间隔式计时，能让ai发的时间不固定产生偏移，更加真实。
-增加主人权限白名单，只有主人能主动命令ai使用修改性工具（发说说、删说说、评论等）。所有用户都可以使用查看说说工具。不填写默认所用用户都有同样权限。
2026.03.18
v1.20：
-学习Maizone成功实现全自动获取和续cookie，移除旧无效的自动获取cookie方式仅保留访客方式保活作为辅助。
-实现定时回复的子回复形式。
v1.30：
特别感谢KiraAI开发者@xxynet对代码的全面改进和优化，
-彻底废弃旧无用的环境依赖和cookie获取方法。
-去除对onebot的http服务开启需求，现在仅只用WebSocket服务即可。
-合并LLoneBot与Napcat的可共用代码，精简冗余。
-webui内可指定适配器名称或自动获取适配器实例。
-persona获取逻辑改为直接从PersonaManager获取。
-尝试解决子回复@昵称格式重复问题。
-专门更新KIraAI本体相关内容以提升对本插件的兼容性。
-修正巨量错误。
2026.05.06
v1.35：
-彻底废弃长时间保活任务，修复了旧版中因保活任务导致的 Unclosed client session 错误。
-每次调用空间功能时，插件会强制从已连接的适配器获取最新 Cookie，确保 Cookie 始终有效。
-根据个人使用经验，修改了默认的定时任务提示词。
2026.06.02
v1.36：
-增加适配2.19.2版本的KiraAI寻找qq适配器的方式，并提供向下兼容。
2026.06.29
v1.37：
-增加黑名单时段配置项，用以模拟作息。
-后台直接生成模式增加可指定LLM。
2026.07.31
v1.40：
-点赞修复：改为 internal_dolike_app → like_cgi_likev6 → mobile like 三级降级；abstime 改用说说发布时间；透传完整 Cookie；tid 规范化；点赞前已赞检测。
-Cookie 刷新稳固化：不再每次调用都强制刷新，改为「启动时 + 周期刷新（默认6h，可配）+ 登录失效特征码触发」三触发；失效时由 HTTP 层回调真正从 OneBot 拉新 Cookie；刷新失败保留并验证旧会话；修复热重载后定时任务丢失的问题。
-配图识图：新增「近期图片清单」，AI 发说说配图时可看到每张图的内容描述并按序号选图（复用核心图片描述缓存，同一张图只识别一次）；发布工具支持直接传聊天图片路径。
-空间识图：新增 qzone_describe_image 工具，AI 可按语义自行决定识别他人说说配图；查看说说输出不再直接刷屏图片URL。
-定时任务指令改为合成内部事件，silent 模式下 AI 静默执行不在群里说话。
-已回复评论与历史说说持久化，重载不丢失。
-修复后台自动评论可能评论自己说说、视频封面空值、feeds3 时间戳类型等问题。
-新增配置：吸附模式开关（默认关，发说说未指定图片时不再盲抓最近图）、允许对自己空间识图开关（默认关）。
-后续打磨：Cookie 机制升级为四层混合（用即刷 10m 节流 + 周期 2h + 失效自救 + 抽风延迟重试，重试均 4 次）；上传接口参数补全（p_uin 等）并支持嵌套错误码识别；空响应/抽风独立递增重试；定时任务指令自带场合信息（群名/昵称）；image_indices 解析失败不再静默发纯文字；吸附开启时清单自动关闭且失败降级纯文字；过期图片 URL 可用 get_msg 续命；图片魔数校验防错误页上传；定时任务 misfire 容错 5 分钟。
2026.08.04
v1.41：
-评论后自动点赞改为插件直接执行（tool_comment 内置，含已赞检测），不再依赖 AI 二次调用点赞工具；修复群聊指令模式下 like_when_comment 不生效的问题。
-新增 backend_persona 配置：后台直接生成模式可指定人设，留空跟随 WebUI 当前激活人设；人设改为每次使用时实时读取，切换激活人设即时生效无需重载。
-定时任务指令的场合信息（群名/昵称）仅发布任务附带；评论/回复任务针对空间说说，不再附带场合避免误导。
-点赞目标 uin 防御性回填，避免详情解析缺字段时 unikey 拼错。

